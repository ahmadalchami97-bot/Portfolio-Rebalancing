# Family Office Portfolio Allocator (KWD Base)

`Family_Office_Portfolio_Allocator.xlsx` — a simple, institutional strategic
asset-allocation, capital-deployment and rebalancing workbook for a multi-million
**KWD** portfolio. Decision-focused, not a transaction tracker. Native Excel only
(formulas, named ranges, structured tables, charts) — **no VBA, no macros**.

## Investment universe
Gold · Swiss Funds · S&P 500 ETFs · US Government Bonds. Base reporting currency: **KWD**.

## Sheets
1. **Settings** — base currency, FX rates, strategic target allocation (with a 100% check),
   tolerance band, minimum action threshold, expected returns, instructions.
2. **Portfolio Inputs** — one row per asset class: market value (FC), currency, auto FX
   lookup, market value (KWD), and the portfolio total. The single source of values.
3. **Dashboard** — total value, current vs target allocation, difference, under-/over-weight
   summaries, and current/target allocation pie charts.
4. **Capital Deployment** — enter new capital; it is allocated to underweight classes first
   (toward target), then by target weights, with the expected post-deployment allocation.
5. **Rebalance** — current vs target value, capital-movement recommendations (overweight→underweight)
   gated by the tolerance band, total to reallocate, and the post-rebalance mix at target weights
   (portfolio total unchanged — a true rebalance, not a capital injection).
6. **Performance** — per-asset and portfolio return/CAGR, a historical allocation scenario,
   and growth-of-capital projections with comparison charts.

## How to use
1. Update market values and currencies on **Portfolio Inputs**.
2. Update FX rates and policy assumptions on **Settings**.
3. Review **Dashboard**, then **Capital Deployment** / **Rebalance**, then **Performance**.

**Yellow cells are inputs; everything else is calculated.** Calculated cells are locked
and inputs are unlocked, so worksheet protection can optionally be switched on
(Review ▸ Protect Sheet) without blocking data entry.

## Default assumptions (editable)
FX: USD/KWD 0.307, CHF/KWD 0.34. Targets: Gold 20% / Swiss 20% / S&P 500 40% / Bonds 20%.
Tolerance band 3%; minimum action 100,000 KWD. Example market values total ≈ 6.27M KWD.
All defaults are placeholders — replace with live figures.

## Regenerating
The workbook is generated by `build_workbook.py`:

```bash
pip install openpyxl
python3 build_workbook.py
```
